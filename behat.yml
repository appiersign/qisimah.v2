default:
    suites:
        default:
            contexts:
                - UserAccountContext
    extensions:
        Laracasts\Behat\ServiceContainer\BehatExtension:
            env_path: .env
        Behat\MinkExtension:
          base_url: http://en.wikipedia.org/
          sessions:
            default:
              laravel: ~